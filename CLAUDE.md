# Yufa ToS Classic — Launcher

Launcher + publishing pipeline for the Yufa | ToS - Classic server. See `README.md` for packages and commands, `CONTEXT.md` for vocabulary, `docs/adr/` for decisions.

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues for `yonlu/tos-yufa-launcher` (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
